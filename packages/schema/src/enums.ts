export enum TeamRole {
  Admin = "admin",
  Team = "team",
}

export enum MarkerStyle {
  Pin = "pin",
}

export enum MapStyle {
  Leaflet = "leaflet",
}

export enum MapMode {
  Earth = "earth",
}

export enum ConfigKey {
  ContestName = "contest_name",
  TickDuration = "tick_duration",
  RoundDuration = "round_duration",
  StartDate = "start_date",
  EndDate = "end_date",
  AttackPoints = "attack_points",
  DefensePoints = "defense_points",
  SlaWeight = "sla_weight",
  FirstBloodBonus = "first_blood_bonus",
  BattleMapStyle = "battle_map_style",
  BattleMapMarkerStyle = "battle_map_marker_style",
  BattleMapLabelVisibility = "battle_map_label_visibility",
  BattleMapColorPalette = "battle_map_color_palette",
  BattleMapHexSize = "battle_map_hex_size",
  BattleMapNetworkForceStrength = "battle_map_network_force_strength",
  BattleMapBackgroundIntensity = "battle_map_background_intensity",
  CheckerPoolSize = "checker_pool_size",
  CheckerTimeout = "checker_timeout",
  FlagLength = "flag_length",
  FlagPrefix = "flag_prefix",
  IsRunning = "is_running",
  CurrentTick = "current_tick",
  CurrentRound = "current_round",
}
